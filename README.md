# MSTodo Wrapper

Minimal Electron wrapper for Microsoft ToDo

## Usage

```bash
# install dependencies
npm install

# run in development
npm start

# build AppImage
npm run dist

# run AppImage
chmod +x MSTodo-<version>.AppImage
./MSTodo-<version>.AppImage
